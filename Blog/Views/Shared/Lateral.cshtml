<aside>
    <div>
        @using (Html.BeginForm("BuscaPeloTermo", "Home", FormMethod.Get, new { @class = "inline-form" }))
        {
            <div class="input-group">
                @Html.TextBox("termo", null, new { @class = "form-control", @plarceholder = "Buscar por..." })
                <span class="input-group-append">
                    <button class="btn btn-primary" type="submit">Ok</button>
                </span>
            </div>
        }

    </div>
    <div id="categorias">
        <h3>Categorias</h3>
    </div>
    <div id="autores">
        <h3>Autores</h3>
    </div>
</aside>